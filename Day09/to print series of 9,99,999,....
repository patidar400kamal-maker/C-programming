//to print series of 9,99,999,....
#include <stdio.h>
void main()
{
    int i, n;
    long num = 0;
    printf("Enter number of terms: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
        num = num * 10 + 9;
        printf("%ld ", num);
    }
}
