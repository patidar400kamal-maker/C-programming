//to check given odd number
#include <stdio.h>
void main()
{
    int i, n;
    printf("Enter how many odd numbers: ");
    scanf("%d", &n);

    for(i = 1; i <= n * 2; i = i + 2)
    {
        printf("%d ", i);
    }
}
