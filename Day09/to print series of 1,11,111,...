// to print series of 1,11,111,...
#include <stdio.h>
void main()
{
    int i, n;
    long num = 0;
    printf("Enter number of terms: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
        num = num * 10 + 1;
        printf("%ld ", num);
    }
}
