//to check number is 3 digit number or not
#include <stdio.h>
void main()
{
    int n;
    printf("Enter number: ");
    scanf("%d",&n);
    if(n>=100 && n<=999)
        printf("3-digit number");
    else
        printf("Not a 3-digit number");
}
