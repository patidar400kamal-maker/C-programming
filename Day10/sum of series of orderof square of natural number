//sum of series of orderof square of natural number
#include <stdio.h>
void main()
{
    int i, n;
    printf("Enter limit: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
        printf("%d ", i * i);
    }
}
